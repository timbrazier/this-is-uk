require File.dirname(__FILE__) + '/../spec_helper'

context "The GudieController" do
  # fixtures :gudies
  controller_name :gudie

  specify "should be a GudieController" do
    controller.should_be_an_instance_of GudieController
  end


  specify "should have more specifications" do
    violated "not enough specs"
  end
end
